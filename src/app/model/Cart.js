export class Cart {
    constructor(id, text, percent, remain, image) {
        this.id = id;
        this.text = text;
        this.percent = percent;
        this.remain = remain;
        this.image = image;
    }
}